# Portfolio image assets

Add the two supplied hero images here with these exact filenames:

- `sairam-portrait.jpg` — the normal portrait
- `sairam-cyber.jpg` — the cybernetic portrait

Both images should use identical dimensions and framing. The portfolio currently expects 1024 × 572 source images.
